{
  "name": "iamwilhelm-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "iamwilhelm.com — the personal site of Michael Wilhelm. Esoteric, initiation-structured, glyph-dithered.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.5.0",
    "astro": "^5.7.0",
    "tone": "^15.1.22"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
